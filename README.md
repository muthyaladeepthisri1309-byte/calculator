# calculator
A simple calculator allows the user to enter two numbers and choose an arithmetic operation such as addition, subtraction, multiplication, or division. It processes the selected operation and displays the calculated result to the user.
